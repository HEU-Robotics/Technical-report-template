# HEU Robotics 技术报告排版规则

本文件适用于整个仓库。AI 在修改报告前必须先阅读本文件。

## 目标

把作者提供的草稿转换为结构清晰、可编译、格式统一的中文 LaTeX 技术报告。作者是内容责任人，AI 是排版与规范执行者。

## 内容边界

- draft/ 中的作者草稿是事实、数据、观点和结论的主要来源。
- 不编造草稿中没有的实验、数字、引用、项目经历、因果关系或结论。
- 不把推测改写成事实，不删除失败现象、限制条件或不确定性。
- 不静默修改数字、单位、样本量、比较对象、时间、名称和版本号。
- 信息不足或不同文件互相冲突时，保留明确占位并向作者指出，不自行猜测。
- 可以修正明显错别字、LaTeX 转义和标点，但不得改变技术含义。
- 原始草稿只读；不要改写、移动或删除 draft/ 中的文件。

## 默认工作范围

- 元数据写入 report-info.tex。
- 正文写入 sections/；可以按内容调整章节数量和标题。
- 图片放入 figures/，只引用实际存在的本地文件。
- 排版样式集中在 styles/heu-technical-report.sty。除非作者明确要求改变模板规范，否则不要修改样式文件。
- main.tex 只负责报告装配，不堆放大段正文。

## 排版规范

- 保持层级简洁：section、subsection、subsubsection 不跳级。
- 标签必须唯一；使用 sec:、fig:、tab:、eq: 前缀。
- 图片和表格在正文中先被解释或引用，再出现。
- 图注只描述图中可见内容及必要条件，不添加未经草稿支持的结论。
- 表格使用 booktabs；单位写入列名，数值精度保持一致。
- 公式中的符号首次出现时应由草稿提供或在正文中定义。
- 所有路径使用相对于仓库根目录的路径，不写盘符或用户目录。
- 保留模板的字体、页边距、页眉页脚、强调色和标题风格。

## 工作步骤

1. 阅读 draft/、report-info.tex、现有 sections/ 和 figures/。
2. 列出草稿内容到章节的对应关系，以及缺失或冲突的信息。
3. 在不改变含义的前提下整理章节、段落、图表和公式。
4. 删除已被正文替换的“待填写”占位，但不要用虚构内容填空。
5. 运行 powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1。
6. 作者确认内容后，运行同一命令并增加 -Strict。
7. 汇报修改了哪些文件、草稿中的哪些内容被放到哪里，以及仍需作者确认的问题。

## 完成标准

- 普通检查和严格检查均通过。
- build/main.pdf 可打开，版面无明显溢出、重叠或空白异常。
- 所有图片路径、交叉引用、图表编号和目录均有效。
- 报告不含“待填写”、TODO、TBD 或示例标签。
- 最终文本的每项事实性主张都能追溯到作者草稿或作者明确确认的材料。
